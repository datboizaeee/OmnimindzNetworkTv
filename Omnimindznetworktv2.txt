<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OmnimindzNetworkTv - Ultimate IPTV Streaming Experience</title>
    <meta name="description" content="Experience premium IPTV streaming with OmnimindzNetworkTv - 50,000+ channels, 8K quality, military-grade security, AI-powered recommendations">
    <meta name="keywords" content="OmnimindzNetworkTv, IPTV, streaming, 4K, 8K, live TV, movies, series, premium channels, cord cutting">
    
    <!-- Advanced SEO Meta Tags -->
    <meta property="og:title" content="OmnimindzNetworkTv - Ultimate IPTV Streaming Experience">
    <meta property="og:description" content="Premium IPTV service with 50K+ channels, 8K streaming, and advanced security features">
    <meta property="og:image" content="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1200 600'%3E%3Cpath fill='%2300ffff' d='M0,300 Q300,150 600,300 T1200,300 V600 H0 Z'/%3E%3C/svg%3E">
    <meta property="og:site_name" content="OmnimindzNetworkTv">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="https://omnimindznetworktv.com">
    
    <!-- Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'GA_MEASUREMENT_ID');
    </script>
    
    <!-- Plausible Analytics -->
    <script defer data-domain="omnimindznetworktv.com" src="https://plausible.io/js/script.js"></script>
    
    <!-- Three.js for 4D Hypercube -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r148/three.min.js"></script>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;500;600;700&display=swap');
        
        :root {
            --primary-neon: #00ffff;
            --secondary-neon: #ff0080;
            --accent-neon: #00ff41;
            --dark-bg: #0a0a0a;
            --darker-bg: #000000;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.2);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Rajdhani', sans-serif;
            line-height: 1.6;
            color: #fff;
            background: var(--darker-bg);
            overflow-x: hidden;
        }

        /* 4D Hypercube Background */
        #hypercube-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            opacity: 0.4;
        }

        /* Animated Background */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: linear-gradient(45deg, #000 0%, #0a0a2e 25%, #1a0a2e 50%, #2e0a1a 75%, #000 100%);
            background-size: 400% 400%;
            animation: gradientShift 15s ease infinite;
        }

        .bg-animation::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: 
                radial-gradient(circle at 25% 25%, var(--primary-neon) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, var(--secondary-neon) 0%, transparent 50%),
                radial-gradient(circle at 50% 50%, var(--accent-neon) 0%, transparent 50%);
            opacity: 0.1;
            animation: pulseGlow 8s ease-in-out infinite alternate;
        }

        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes pulseGlow {
            0% { opacity: 0.1; }
            100% { opacity: 0.3; }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Glass Morphism Effect */
        .glass {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0, 255, 255, 0.1);
        }

        /* Header */
        header {
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            padding: 1rem 0;
            transition: all 0.3s ease;
        }

        header.scrolled {
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--primary-neon);
            box-shadow: 0 0 30px rgba(0, 255, 255, 0.3);
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Orbitron', monospace;
            font-size: 1.8rem;
            font-weight: 900;
            color: var(--primary-neon);
            text-decoration: none;
            text-shadow: 0 0 20px var(--primary-neon);
            animation: logoGlow 2s ease-in-out infinite alternate;
        }

        @keyframes logoGlow {
            0% { text-shadow: 0 0 20px var(--primary-neon); }
            100% { text-shadow: 0 0 30px var(--primary-neon), 0 0 40px var(--primary-neon); }
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            color: #fff;
            text-decoration: none;
            padding: 0.5rem 1.5rem;
            border-radius: 25px;
            transition: all 0.3s ease;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
            position: relative;
            overflow: hidden;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, var(--primary-neon), transparent);
            transition: left 0.5s;
        }

        .nav-links a:hover::before {
            left: 100%;
        }

        .nav-links a:hover {
            color: var(--primary-neon);
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
            transform: translateY(-2px);
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="%2300ffff" stroke-width="0.5" opacity="0.3"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            animation: gridMove 20s linear infinite;
        }

        @keyframes gridMove {
            0% { transform: translate(0, 0); }
            100% { transform: translate(10px, 10px); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero-content h1 {
            font-family: 'Orbitron', monospace;
            font-size: 4rem;
            font-weight: 900;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon), var(--accent-neon));
            background-size: 200% 200%;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            animation: textShine 3s ease-in-out infinite;
            text-shadow: 0 0 50px rgba(0, 255, 255, 0.5);
        }

        @keyframes textShine {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .hero-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            color: var(--primary-neon);
            text-transform: uppercase;
            letter-spacing: 3px;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {
            0% { opacity: 0.7; }
            50% { opacity: 1; }
            100% { opacity: 0.7; }
        }

        .cta-button {
            display: inline-block;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon));
            color: #000;
            padding: 1.5rem 3rem;
            text-decoration: none;
            border-radius: 50px;
            font-weight: bold;
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            box-shadow: 0 0 30px rgba(0, 255, 255, 0.5);
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            transition: left 0.5s;
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 10px 40px rgba(0, 255, 255, 0.8);
        }

        /* Features Section */
        .features {
            padding: 8rem 0;
            position: relative;
        }

        .section-title {
            font-family: 'Orbitron', monospace;
            text-align: center;
            font-size: 3rem;
            margin-bottom: 4rem;
            color: var(--primary-neon);
            text-transform: uppercase;
            letter-spacing: 3px;
            position: relative;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 3px;
            background: linear-gradient(90deg, var(--primary-neon), var(--secondary-neon));
            border-radius: 2px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 3rem;
        }

        .feature-card {
            padding: 2.5rem;
            text-align: center;
            transition: all 0.3s ease;
            position: relative;
            border-radius: 20px;
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.1), rgba(255, 0, 128, 0.1));
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 255, 255, 0.3);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            border-radius: 20px;
            padding: 2px;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon), var(--accent-neon));
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-card:hover {
            transform: translateY(-15px) rotateX(5deg);
            box-shadow: 0 30px 60px rgba(0, 255, 255, 0.3);
        }

        .feature-icon {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            color: var(--primary-neon);
            filter: drop-shadow(0 0 20px var(--primary-neon));
        }

        .feature-card h3 {
            font-family: 'Orbitron', monospace;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--primary-neon);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        /* Packages Section */
        .packages {
            padding: 8rem 0;
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.4));
        }

        .packages-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 3rem;
            margin-top: 4rem;
        }

        .package-card {
            padding: 3rem;
            text-align: center;
            position: relative;
            border-radius: 25px;
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.1), rgba(255, 0, 128, 0.1));
            backdrop-filter: blur(30px);
            border: 2px solid transparent;
            transition: all 0.4s ease;
            overflow: hidden;
        }

        .package-card::before {
            content: '';
            position: absolute;
            inset: 0;
            padding: 2px;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon), var(--accent-neon));
            border-radius: 25px;
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .package-card.featured {
            transform: scale(1.05);
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.2), rgba(255, 0, 128, 0.2));
        }

        .package-card.featured::before {
            opacity: 1;
        }

        .package-card:hover::before {
            opacity: 1;
        }

        .package-card:hover {
            transform: translateY(-20px) scale(1.02);
            box-shadow: 0 40px 80px rgba(0, 255, 255, 0.4);
        }

        .package-title {
            font-family: 'Orbitron', monospace;
            font-size: 2rem;
            color: var(--primary-neon);
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .package-price {
            font-family: 'Orbitron', monospace;
            font-size: 3.5rem;
            color: var(--secondary-neon);
            font-weight: bold;
            margin-bottom: 0.5rem;
            text-shadow: 0 0 20px var(--secondary-neon);
        }

        .package-features {
            list-style: none;
            margin: 2rem 0;
            text-align: left;
        }

        .package-features li {
            color: #fff;
            padding: 0.8rem 0;
            border-bottom: 1px solid rgba(0, 255, 255, 0.2);
            position: relative;
            padding-left: 2rem;
        }

        .package-features li::before {
            content: '⚡';
            position: absolute;
            left: 0;
            color: var(--accent-neon);
            font-size: 1.2rem;
        }

        .package-button {
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon));
            color: #000;
            padding: 1.2rem 2.5rem;
            border: none;
            border-radius: 50px;
            font-weight: bold;
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
            font-family: 'Orbitron', monospace;
            position: relative;
            overflow: hidden;
        }

        .package-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            transition: left 0.5s;
        }

        .package-button:hover::before {
            left: 100%;
        }

        .package-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0, 255, 255, 0.6);
        }

        /* Newsletter Popup */
        .newsletter-popup {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0);
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.15), rgba(255, 0, 128, 0.15));
            backdrop-filter: blur(30px);
            border: 2px solid var(--primary-neon);
            border-radius: 25px;
            padding: 3rem;
            z-index: 10000;
            width: 90%;
            max-width: 500px;
            text-align: center;
            box-shadow: 0 0 50px rgba(0, 255, 255, 0.5);
            transition: all 0.4s ease;
            opacity: 0;
        }

        .newsletter-popup.active {
            transform: translate(-50%, -50%) scale(1);
            opacity: 1;
        }

        .newsletter-popup h3 {
            font-family: 'Orbitron', monospace;
            color: var(--primary-neon);
            font-size: 1.8rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
        }

        .newsletter-form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 2rem;
        }

        .newsletter-input {
            background: rgba(0, 0, 0, 0.5);
            border: 2px solid var(--primary-neon);
            border-radius: 50px;
            padding: 1rem 1.5rem;
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .newsletter-input:focus {
            outline: none;
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
            border-color: var(--secondary-neon);
        }

        .newsletter-input::placeholder {
            color: #888;
        }

        .newsletter-submit {
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon));
            color: #000;
            border: none;
            border-radius: 50px;
            padding: 1.2rem 2rem;
            font-weight: bold;
            font-size: 1.1rem;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Orbitron', monospace;
        }

        .newsletter-submit:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0, 255, 255, 0.6);
        }

        .newsletter-close {
            position: absolute;
            top: 15px;
            right: 15px;
            background: none;
            border: none;
            color: var(--primary-neon);
            font-size: 2rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .newsletter-close:hover {
            color: var(--secondary-neon);
            transform: rotate(90deg);
        }

        .popup-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            z-index: 9999;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .popup-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        /* Payment Modal */
        .payment-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(20px);
            z-index: 10002;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .payment-modal.active {
            opacity: 1;
            visibility: visible;
        }

        .payment-content {
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.15), rgba(255, 0, 128, 0.15));
            backdrop-filter: blur(30px);
            border: 2px solid var(--primary-neon);
            border-radius: 25px;
            padding: 3rem;
            max-width: 500px;
            width: 90%;
            position: relative;
            box-shadow: 0 0 50px rgba(0, 255, 255, 0.5);
            text-align: center;
        }

        .payment-close {
            position: absolute;
            top: 15px;
            right: 15px;
            background: none;
            border: none;
            color: var(--primary-neon);
            font-size: 2rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .payment-close:hover {
            color: var(--secondary-neon);
            transform: rotate(90deg);
        }

        .payment-options {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 1rem;
            margin: 2rem 0;
        }

        .payment-option {
            background: rgba(0, 0, 0, 0.5);
            border: 2px solid var(--primary-neon);
            border-radius: 15px;
            padding: 1.5rem 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.5rem;
        }

        .payment-option:hover {
            background: rgba(0, 255, 255, 0.1);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 255, 255, 0.3);
        }

        .payment-icon {
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        .payment-name {
            font-size: 0.9rem;
            font-weight: bold;
            color: var(--primary-neon);
        }

        .payment-instructions {
            background: rgba(0, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            margin-top: 1rem;
            text-align: left;
        }

        /* WhatsApp QR Code Modal */
        .qr-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(20px);
            z-index: 10001;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .qr-modal.active {
            opacity: 1;
            visibility: visible;
        }

        .qr-content {
            background: linear-gradient(135deg, rgba(0, 255, 255, 0.15), rgba(255, 0, 128, 0.15));
            backdrop-filter: blur(30px);
            border: 2px solid var(--primary-neon);
            border-radius: 25px;
            padding: 3rem;
            text-align: center;
            max-width: 400px;
            width: 90%;
            position: relative;
            box-shadow: 0 0 50px rgba(0, 255, 255, 0.5);
        }

        .qr-close {
            position: absolute;
            top: 15px;
            right: 15px;
            background: none;
            border: none;
            color: var(--primary-neon);
            font-size: 2rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .qr-close:hover {
            color: var(--secondary-neon);
            transform: rotate(90deg);
        }

        .qr-code-image {
            width: 250px;
            height: 250px;
            background: white;
            border-radius: 15px;
            margin: 1rem auto;
            padding: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        /* WhatsApp Widget Enhanced */
        .whatsapp-widget {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
            display: flex;
            flex-direction: column;
            gap: 10px;
            align-items: flex-end;
        }

        .whatsapp-button, .qr-button {
            width: 60px;
            height: 60px;
            background: linear-gradient(45deg, #25D366, #128C7E);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.8rem;
            text-decoration: none;
            box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
            transition: all 0.3s ease;
            animation: whatsappPulse 2s ease-in-out infinite;
            border: none;
            cursor: pointer;
        }

        .qr-button {
            width: 50px;
            height: 50px;
            font-size: 1.4rem;
            background: linear-gradient(45deg, var(--primary-neon), var(--secondary-neon));
            animation-delay: 0.5s;
        }

        @keyframes whatsappPulse {
            0% { box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4); }
            50% { box-shadow: 0 4px 30px rgba(37, 211, 102, 0.8); }
            100% { box-shadow: 0 4px 20px rgba(37, 211, 102,